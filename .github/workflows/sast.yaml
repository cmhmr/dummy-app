name: fortify sast scan
on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  security-scan:
    uses: cmhmr/appsec-workflows/.github/workflows/fortify-sast.yaml@main
    secrets: inherit